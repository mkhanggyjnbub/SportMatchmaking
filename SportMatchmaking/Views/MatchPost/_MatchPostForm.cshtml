@model SportMatchmaking.Models.MatchPostFormVM

@{
    var feeFieldName = nameof(Model.FeePerPerson);
    var feeInputValue = ViewData.ModelState.TryGetValue(feeFieldName, out var feeEntry)
        && !string.IsNullOrWhiteSpace(feeEntry?.AttemptedValue)
            ? feeEntry!.AttemptedValue
            : Model.FeePerPerson?.ToString("0.############################", System.Globalization.CultureInfo.InvariantCulture);
}

<div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

<div class="row g-3">
    <div class="col-lg-7">
        <label asp-for="SportId" class="form-label">Môn thể thao</label>
        <select asp-for="SportId" asp-items="Model.SportOptions" class="form-select">
            <option value="">-- Chọn môn --</option>
        </select>
        <span asp-validation-for="SportId" class="text-danger"></span>
    </div>

    <div class="col-lg-5">
        <label class="form-label">Ảnh theo môn</label>
        <div class="match-sport-preview-card">
            <img src="@Model.SelectedSportImageUrl"
                 alt="Ảnh môn thể thao"
                 class="match-sport-preview-image @(string.IsNullOrWhiteSpace(Model.SelectedSportImageUrl) ? "d-none" : "")"
                 data-sport-preview-image />
            <div class="match-sport-preview-empty @(string.IsNullOrWhiteSpace(Model.SelectedSportImageUrl) ? "" : "d-none")"
                 data-sport-preview-empty>
                Chọn môn để xem ảnh tự động.
            </div>
        </div>
        <input type="text"
               class="form-control mt-2"
               value="@Model.SelectedSportImageUrl"
               placeholder="Link ảnh sẽ tự lấy theo môn đã chọn"
               readonly
               data-sport-preview-url />
    </div>

    <div class="col-md-6">
        <label asp-for="MatchType" class="form-label">Loại trận</label>
        <select asp-for="MatchType" asp-items="Model.MatchTypeOptions" class="form-select"></select>
        <span asp-validation-for="MatchType" class="text-danger"></span>
    </div>

    <div class="col-12">
        <label asp-for="Title" class="form-label">Tiêu đề</label>
        <input asp-for="Title" class="form-control" placeholder="Ví dụ: Cần thêm 2 bạn đánh cầu tới đây này" />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>

    <div class="col-md-6">
        <label asp-for="StartTime" class="form-label">Bắt đầu</label>
        <input asp-for="StartTime" type="datetime-local" asp-format="{0:yyyy-MM-ddTHH:mm}" class="form-control" />
        <span asp-validation-for="StartTime" class="text-danger"></span>
    </div>

    <div class="col-md-6">
        <label asp-for="EndTime" class="form-label">Kết thúc</label>
        <input asp-for="EndTime" type="datetime-local" asp-format="{0:yyyy-MM-ddTHH:mm}" class="form-control" />
        <span asp-validation-for="EndTime" class="text-danger"></span>
    </div>

    <div class="col-md-8">
        <label asp-for="LocationText" class="form-label">Địa điểm (không bắt buộc)</label>
        <input asp-for="LocationText" class="form-control" placeholder="Sân, trung tâm hoặc địa chỉ cụ thể" />
        <span asp-validation-for="LocationText" class="text-danger"></span>
        <div class="form-text">Ban co the nhap tay hoac bam nut chon tren ban do de dien dia chi tu dong.</div>
    </div>

    <div class="col-md-4">
        <label asp-for="GoogleMapsUrl" class="form-label">Google Maps URL</label>
        <input asp-for="GoogleMapsUrl" class="form-control" placeholder="https://maps.google.com/..." />
        <span asp-validation-for="GoogleMapsUrl" class="text-danger"></span>
        <div class="d-flex flex-wrap gap-2 mt-2">
            <button type="button"
                    class="btn btn-outline-primary btn-sm"
                    data-map-picker-open>
                Chon tren ban do
            </button>
            <a href="#"
               class="btn btn-outline-secondary btn-sm disabled"
               target="_blank"
               rel="noopener noreferrer"
               aria-disabled="true"
               data-map-picker-open-link>
                Mo link map
            </a>
        </div>
    </div>

    <div class="col-md-6">
        <label asp-for="City" class="form-label">Thành phố (không bắt buộc)</label>
        <input asp-for="City" class="form-control" />
        <span asp-validation-for="City" class="text-danger"></span>
    </div>

    <div class="col-md-6">
        <label asp-for="District" class="form-label">Quận/Huyện (không bắt buộc)</label>
        <input asp-for="District" class="form-control" />
        <span asp-validation-for="District" class="text-danger"></span>
    </div>

    <div class="col-md-3">
        <label asp-for="SkillMin" class="form-label">Skill tối thiểu</label>
        <input asp-for="SkillMin" type="number" min="1" max="10" class="form-control" />
        <span asp-validation-for="SkillMin" class="text-danger"></span>
    </div>

    <div class="col-md-3">
        <label asp-for="SkillMax" class="form-label">Skill tối đa</label>
        <input asp-for="SkillMax" type="number" min="1" max="10" class="form-control" />
        <span asp-validation-for="SkillMax" class="text-danger"></span>
    </div>

    <div class="col-md-3">
        <label asp-for="SlotsNeeded" class="form-label">Số slot cần thêm</label>
        <input asp-for="SlotsNeeded" type="number" min="1" max="50" class="form-control" />
        <span asp-validation-for="SlotsNeeded" class="text-danger"></span>
    </div>

    <div class="col-md-3">
        <label asp-for="FeePerPerson" class="form-label">Phí / người</label>
        <input asp-for="FeePerPerson" type="number" min="0" step="1000" value="@feeInputValue" class="form-control" />
        <span asp-validation-for="FeePerPerson" class="text-danger"></span>
    </div>

    <div class="col-md-6">
        <label asp-for="ExpiresAt" class="form-label">Hạn chốt</label>
        <input asp-for="ExpiresAt" type="datetime-local" asp-format="{0:yyyy-MM-ddTHH:mm}" class="form-control" />
        <span asp-validation-for="ExpiresAt" class="text-danger"></span>
    </div>

    <div class="col-md-6 d-flex align-items-end">
        <div class="form-check mb-2">
            <input asp-for="IsUrgent" class="form-check-input" />
            <label asp-for="IsUrgent" class="form-check-label">Đánh dấu gấp</label>
        </div>
    </div>

    <div class="col-12">
        <label asp-for="Description" class="form-label">Mô tả</label>
        <textarea asp-for="Description" rows="5" class="form-control" placeholder="Mô tả trình độ mong muốn, chi phí, lưu ý thêm..."></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>
</div>

<div class="modal fade" id="matchPostMapPickerModal" tabindex="-1" aria-labelledby="matchPostMapPickerLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="matchPostMapPickerLabel">Chon dia diem tren ban do</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="row g-3">
                    <div class="col-lg-8">
                        <div class="input-group mb-3">
                            <input type="text"
                                   class="form-control"
                                   placeholder="Tim san, duong, khu vuc..."
                                   data-map-picker-search />
                            <button type="button" class="btn btn-outline-secondary" data-map-picker-search-button>
                                Tim
                            </button>
                            <button type="button" class="btn btn-outline-primary" data-map-picker-current-location>
                                Vi tri hien tai
                            </button>
                        </div>

                        <div class="match-post-map-status alert alert-light border mb-3" data-map-picker-status>
                            Bam vao ban do de chon vi tri, hoac tim theo ten dia diem.
                        </div>

                        <div class="match-post-map-canvas" data-map-picker-canvas></div>
                    </div>

                    <div class="col-lg-4">
                        <div class="match-post-map-summary">
                            <div class="match-post-map-summary-label">Dia chi se dien vao form</div>
                            <div class="match-post-map-summary-value" data-map-picker-address>
                                Chua chon vi tri.
                            </div>
                        </div>

                        <div class="match-post-map-summary">
                            <div class="match-post-map-summary-label">Thanh pho</div>
                            <div class="match-post-map-summary-value" data-map-picker-city>-</div>
                        </div>

                        <div class="match-post-map-summary">
                            <div class="match-post-map-summary-label">Quan / Huyen</div>
                            <div class="match-post-map-summary-value" data-map-picker-district>-</div>
                        </div>

                        <div class="match-post-map-summary">
                            <div class="match-post-map-summary-label">Toa do</div>
                            <div class="match-post-map-summary-value" data-map-picker-coordinates>-</div>
                        </div>

                        <div class="form-text">
                            He thong se sinh link Google Maps tu vi tri ban chon va dien vao cac truong dia diem.
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Dong</button>
                <button type="button" class="btn btn-primary" disabled data-map-picker-apply>Ap dung vi tri</button>
            </div>
        </div>
    </div>
</div>

<script>
    (() => {
        const sportSelect = document.getElementById("SportId");
        if (!sportSelect) {
            return;
        }

        const sportImageMap = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.SportImageMap));
        const previewImage = document.querySelector("[data-sport-preview-image]");
        const previewEmpty = document.querySelector("[data-sport-preview-empty]");
        const previewUrl = document.querySelector("[data-sport-preview-url]");

        const renderSportPreview = () => {
            const selectedOption = sportSelect.options[sportSelect.selectedIndex];
            const sportId = sportSelect.value;
            const sportName = selectedOption ? selectedOption.text.trim() : "";
            const imageUrl = sportImageMap[sportId] || "";

            if (previewUrl) {
                previewUrl.value = imageUrl;
            }

            if (imageUrl) {
                previewImage?.setAttribute("src", imageUrl);
                previewImage?.setAttribute("alt", sportName ? `Ảnh môn ${sportName}` : "Ảnh môn thể thao");
                previewImage?.classList.remove("d-none");
                previewEmpty?.classList.add("d-none");
                return;
            }

            previewImage?.classList.add("d-none");
            previewImage?.removeAttribute("src");
            if (previewEmpty) {
                previewEmpty.textContent = sportId
                    ? `${sportName || "Môn này"} chưa có ảnh được cấu hình.`
                    : "Chọn môn để xem ảnh tự động.";
                previewEmpty.classList.remove("d-none");
            }
        };

        sportSelect.addEventListener("change", renderSportPreview);
        renderSportPreview();
    })();
</script>
