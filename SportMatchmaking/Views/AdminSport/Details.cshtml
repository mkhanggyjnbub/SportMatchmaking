@model BusinessObjects.Sport

@{
    ViewData["Title"] = "Chi ti?t môn th? thao";
}

<h2 class="admin-page-title mb-1">Chi ti?t môn th? thao</h2>
<p class="admin-page-subtitle mb-4">Thông tin ð?y ð? c?a môn th? thao trong h? th?ng.</p>

<div class="card admin-card">
    <div class="card-body">
        <dl class="row mb-0">
            <dt class="col-sm-3">Sport ID</dt>
            <dd class="col-sm-9 fw-semibold">@Model.SportId</dd>

            <dt class="col-sm-3">Tên môn th? thao</dt>
            <dd class="col-sm-9">@Model.Name</dd>

            <dt class="col-sm-3">Team Min</dt>
            <dd class="col-sm-9">@Model.TeamMin</dd>

            <dt class="col-sm-3">Team Max</dt>
            <dd class="col-sm-9">@Model.TeamMax</dd>

            <dt class="col-sm-3">Image ID</dt>
            <dd class="col-sm-9">@Model.ImageId</dd>

            <dt class="col-sm-3">Image URL</dt>
            <dd class="col-sm-9 text-break">@(Model.Image?.ImageUrl ?? "")</dd>

            <dt class="col-sm-3">Created At</dt>
            <dd class="col-sm-9">@Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</dd>
        </dl>
    </div>
</div>

<div class="mt-3 d-flex gap-2 flex-wrap">
    <a asp-action="Edit" asp-route-id="@Model.SportId" class="btn btn-warning">S?a</a>
    <a asp-action="Index" class="btn btn-secondary">Quay l?i</a>
</div>
